#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright 2024 Celestica Inc. All Rights Reserved.
# GNU General Public License v3.0+
# (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

#############################################
#                WARNING                    #
#############################################
#
# This file is auto generated by the resource
#   module builder playbook.
#
# Do not edit this file manually.
#
# Changes to this file will be over written
#   by the resource module builder.
#
# Changes should be made in the model used to
#   generate this file or in the resource module
#   builder template.
#
#############################################

"""
The module file for feature
"""

from __future__ import absolute_import, division, print_function
__metaclass__ = type

ANSIBLE_METADATA = {
    'metadata_version': '1.1',
    'status': ['preview'],
    'supported_by': 'community',
    'license': 'Apache 2.0'
}

DOCUMENTATION = """
---
module: feature
version_added: 1.0.0
short_description: Resource module to configure features.
description:
  - This module configures and manages features on devices running Celestica SONiC NOS.
author: Celestica SONiC Ansible Team (@dharmaraj-gurusamy)
notes:
  - Tested against Advanced Celestica SONiC, release 2.0.
  - This module works with connection C(network_cli).
options:
  config:
    description:
      - Specifies configurations related to feature management.
    type: dict
    suboptions:
      # Features config
      feature:
        description:
          - Feature configurations.
          - List of features are static, hence delete is not allowed.
        type: list
        elements: dict
        suboptions:
          name:
            description:
              - Name of the feature to be managed.
            type: str
            choices: [bgp, dhcp_relay, eventd, gnmi, iccpd, lldp, macsec, mgmt-framework, mstp, mux, nat, pmon, radv, sflow, snmp, swss, syncd, teamd, bgpd, ospf6d, ospfd, staticd]
            required: true
          state:
            description:
              - Enable or disable feature state.
            type: bool
          auto-restart:
            description:
              - Enable or disable auto-restart state.
            type: bool
          fallback:
            description:
              - Enable or disable fallback state.
            type: bool
          owner:
            description:
              - Configure the owner who manages the feature.
            type: str
            choices: [local, kube]

  state:
    description:
      - The state of the configuration after module completion.
      - C(merged) - Merge specified configuration with on-device running configuration.
      - C(deleted) - Delete the specified on-device running configuration or applies the default value.
    type: str
    choices:
      - merged
      - deleted
    default: merged
"""
EXAMPLES = """
# Using state : merged
#
# Before state:
# -------------
#
# Celestica-DS1000# show running-config
# !
# feature gnmi state enabled auto-restart enabled
# feature macsec state enabled auto-restart enabled owner local
# feature sflow state disabled auto-restart enabled
# !
# Celestica-DS1000#
#
# Play:
# ----
#
    - name: Merge Feature Configuration
      celestica.sonic.feature:
        config:
          feature:
            - name: sflow
              state: true
              auto-restart: false
              fallback: true
              owner: local
            - name: gnmi
              owner: kube
            - name: macsec
              state: false
              fallback: true
        state: merged

# After state:
# ------------
#
# Celestica-DS1000# show running-config
# !
# feature gnmi state enabled auto-restart enabled owner kube
# feature macsec state disabled auto-restart enabled owner local fallback on
# feature sflow state enabled auto-restart disabled owner local fallback on
# !
# Celestica-DS1000#


"""
RETURN = """
before:
  description: The configuration prior to the model invocation.
  returned: always
  sample: >
    The configuration returned will always be in the same format
     of the parameters above.
after:
  description: The resulting configuration model invocation.
  returned: when changed
  sample: >
    The configuration returned will always be in the same format
     of the parameters above.
commands:
  description: The set of commands pushed to the remote device.
  returned: always
  type: list
  sample: ['command 1', 'command 2', 'command 3']
"""


from ansible.module_utils.basic import AnsibleModule
from ansible_collections.celestica.sonic.plugins.module_utils.network.sonic.argspec.feature.feature import FeatureArgs
from ansible_collections.celestica.sonic.plugins.module_utils.network.sonic.config.feature.feature import Feature


def main():
    """
    Main entry point for module execution

    :returns: the result form module invocation
    """
    module = AnsibleModule(argument_spec=FeatureArgs.argument_spec,
                           supports_check_mode=True)

    result = Feature(module).execute_module()
    module.exit_json(**result)


if __name__ == '__main__':
    main()
