---
# Command modes
command_modes:
  - sonic_cli

# sonic-cli translation
sonic_cli:
  # BGP community list config
  community-list-config:
    subcommand:
      - bgp=LIST:community-list&KEYS:$type|$id community-list $type=OPTIONAL&VALUE_CHECK_METHOD:!integer $id seq=LIST:sequence-numbers&KEYS:$seq $seq $action $number
